# shared/hooks

Hooks React réutilisables et transverses à plusieurs modules (ex. `use-media-query`,
`use-debounced-value`). Un hook spécifique à un seul module doit vivre dans
`features/<module>/hooks/`, pas ici.

Dossier volontairement vide à ce stade des fondations.
