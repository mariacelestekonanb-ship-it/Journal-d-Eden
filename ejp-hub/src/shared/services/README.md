# shared/services

Services transverses partagés par plusieurs modules (ex. envoi d'e-mails,
génération de fichiers). Un service propre à un seul module doit vivre dans
`features/<module>/services/`, pas ici.

Dossier volontairement vide à ce stade des fondations.
